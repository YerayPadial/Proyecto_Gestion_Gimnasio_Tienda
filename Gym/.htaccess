<IfModule mod_rewrite.c>
  RewriteEngine On
  RewriteBase /gym/
  RewriteRule ^index\.html$ - [L]
  RewriteCond %{REQUEST_FILENAME} !-f
  RewriteCond %{REQUEST_FILENAME} !-d
  RewriteRule . /gym/index.html [L]
</IfModule>
